# openrag

The RAG layer beneath any chatbot. This package is the core: the public API, the planner,
search and fusion, Markdown parsing, the default store, and the JSON trace.

Status: skeleton. The interfaces reflect the recorded design decisions; the
implementations land in M1–M5.
